// Object
const user: {
    firstName: string;
    middleName?: string; // Optional property (type)
    lastName: string;
    readonly isEmployed: true; // Literal type - read-only property
} = {
    firstName: 'Rifat',
    lastName: 'Tusar',
    isEmployed: true,
};
